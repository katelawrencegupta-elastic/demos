{
  "description": "Stamp ARCH-02 traces. Keep sampled / short retention.",
  "processors": [
    { "set": { "field": "event.ingested", "value": "{{_ingest.timestamp}}" } },
    { "set": { "field": "labels.workshop", "value": "arch-02" } },
    { "set": { "field": "labels.retention_class", "value": "traces_sampled" } }
  ]
}
