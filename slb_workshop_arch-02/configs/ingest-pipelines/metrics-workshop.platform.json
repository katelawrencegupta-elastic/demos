{
  "description": "Stamp ARCH-02 platform metrics.",
  "processors": [
    { "set": { "field": "event.ingested", "value": "{{_ingest.timestamp}}" } },
    { "set": { "field": "labels.workshop", "value": "arch-02" } },
    { "set": { "field": "labels.retention_class", "value": "platform_metrics" } }
  ]
}
