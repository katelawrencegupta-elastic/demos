# Use container name when logstash runs on the shared `demos` docker network.
NETFLOW_TARGET=logstash
NETFLOW_PORT=2055
NETFLOW_INTERVAL=1
NETFLOW_FLOWS_PER_PACKET=8
NETFLOW_IMAGE=demos-netflow
NETFLOW_CONTAINER_NAME=netflow-generator
